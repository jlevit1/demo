@model Job

<div class="container col-md-12 card bg-light">
    <form asp-controller="Home" asp-action="Apply" method="post" enctype="multipart/form-data">
        <div class="w-100">
            <h1 class="text text-center text-primary">Name: @Model.Name</h1>
            <h3 class="text-text-success">Skills: @Model.Skills</h3>
            <h3 class="text text-success">Requiment: @Model.Requiment</h3>
            <h3 class="text text-success">Salary: $@Model.Salary</h3>
            <h3 class="text text-success">Description: @Model.Description</h3>
       @*      <div class="form-group text-left">
               <div class="form-group">
                   <input asp-for="@View"  class="form-control" type="file" />
               </div>
               Choose your CV to apply
            </div> *@
            <div class="form-group text-right">
                <input type="hidden" name="id" value="@Model.Id" />
                <input type="submit" value="Apply" class="btn btn-primary" />
            </div>
        </div>
    </form>
</div>